import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  resourcesTable: {
    width: '100%',
  },
  marginRight: {
      marginRight: '15px'
  }
});

export default useStyles;
